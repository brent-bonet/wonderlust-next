@AGENTS.md
After every edit, run the tests and the type check; treat any failure as a blocker and fix it before moving on. 
Run the build after completing each logical change and before every commit — a build failure is also a blocker.